
Make me a webapp where i can select a C# folder that is a code repository. Then the webapp scans and reads all files and presents a directory view in the sidebar on the left- this has all the cs classes in a folder view where i can expand folders to view the cs files inside. If there are two classes inside a file, you have to present them as though they are different files. This is a class view, organized by folder. In the directory view you can also expand each class to see all the methods behind. This information should be stored in memory or optionally in a json (that the user can update with a button press).

The 