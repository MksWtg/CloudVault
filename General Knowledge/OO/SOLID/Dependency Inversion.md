High level classes should not depend on low level classes. Both should depend on abstractions (e.g. interfaces, or abstract classes).


>Note: Dependency Inversion is commonly achieved through dependency injection
