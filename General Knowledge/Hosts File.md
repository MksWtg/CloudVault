A file located at `C:\Windows\System32\drivers\etc` for windows where you can specify what IPs specific hosts map to.

You can use it to pretend localhost is another machine, e.g. for reenabling NTLM during development.