In the context of databases, a relation is a mathematical concept that corresponds to a table. Formally:

A relation is a set of tuples (rows), where each tuple has the same set of attributes (columns).
Each attribute has a domain, i.e., a specific type or set of valid values.
All tuples in a relation are unique, no duplicates are allowed.
The order of tuples and attributes does not matter; only the content matters.