In `man`, the numbers like **1, 2, 3** are **manual sections**. They tell you _which category of documentation_ you are looking at, because many names are reused (e.g., `printf` can be a command or a C function).


1 is user commands
2 is sys calls
3 is library funcitons

use man 2 open or man 3 open

there are functions to get time, set time and 