A command line interface (CLI) is a text based UI used to interact with a computer's OS or software by typing in commands. 

A command line typically runs inside a terminal emulator.

Here is a [[Terminal Emulator]] running the `cmd.exe` [[Shell]], which the user can interact with via a command line.
![[Pasted image 20260217110133.png]]
