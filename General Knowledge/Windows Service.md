 What is a Windows Service?
 
 A Windows Service is a long-running background process on a Windows server that starts automatically with the OS, runs without a logged-in user, and keeps running 24/7.
 
 Like a daemon on Linux. 