
`async` and `await` are two keywords that are used to write asynchronous code in C#.

`async` is a modifier for methods. It signals to the compiler that the code in the method should be asynchronous (that it may use the `await` keyword). You cannot use `await` outside of `async` code.