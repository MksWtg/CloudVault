Backing is something that happens in C# when syntactic sugar expands to more code that is not shown to make everything cleaner.

The autogenerated code is called "backing".

## Examples

Prerequisites: [[Events]], [[Getter-Setters]]

Every event has a backing delegate.
Every getter has a backing field.

TODO: fix the above examples.