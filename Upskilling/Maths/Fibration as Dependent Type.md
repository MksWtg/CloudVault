
TODO fix this article, i dont know what a dependent ytpe, type family, any of it is

A fibration is a map $f : B \to A$ that satisfies the homotopy lifting property.

$B$ is a collection of fibers, each gets sent to a point in A.

A dependent type is a type that depends on a value $a : A \vdash B(a)$ 

Each point `y` in the space `Y` is maps to an element in `X`.