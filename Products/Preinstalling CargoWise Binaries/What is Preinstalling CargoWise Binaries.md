
We currently use appmanager to install cargowise
Appmanager is dangerous (because of permission issues)

We want to preinstall cargowise versions on all cargowise servers. So we don't need appmanager anymore.