
Prerequisites: [[What is Process Controller Orchestrator]]

First, the host that the PC will be installed on needs to have cargowise preinstalled. the orchestrator checks an environmentvalid flag on each PC host, which is true if CargoWise.Start.exe (the bootstrapped) already exists at 

`C:\Program Files\WiseTech Global\CargoWise\CargoWise.Start.exe`