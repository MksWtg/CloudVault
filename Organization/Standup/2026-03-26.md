Using brain:
1) perform functional test: restore db with user repository with mapped server principal and db principal to the server and see what happens
	1) what we expect: db prinicpal can be recreated but server principal cannot be (is null, not mapped)