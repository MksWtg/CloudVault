Functional review checklist
1. creating a PR against main/master triggers the Build and Test workflow automatically
 2. manually triggering `Build and Test` via workflow_dispatch works
 3. build succeeds and the exe is stamped with a GitVersion-derived version (not 0.0.0)
 4. tests run and pass
 5. SQL server is detected/started and SQL_SERVER_INSTANCE is correctly passed to the tests
 6. test results .trx are uploaded as an artefact
 7. code coverage report is generated and uploaded as an artifact
 8. trx converted to html and uploaded even if tests fail

patch release workflow
- some info: the `releases/release-X.Y` branch is created when a major or minor release workflow is run. if a bug needs fixing, you can cherry pick commits from main onto that branch and then run patch release from that branch to publish 1.2.1, 1.2.2. each release receives independent patches.
9. running patch release from a releases/release-X.Y branch succeeds
10. running patch release from any other branch fails with a clear error message
11. MSI is signed
12. exe is signed
13. choco .nupkg is created with the correct version
14. .nupkg is pushed to `CHOCOLATEY_FEED_URL`
15. GitHub release is created with the msi and nupkg attached

minor workflow
16. Running minor release from main succeeds
17. Running minor release from a non-main branch fails with a clear error
18. A releases/release-X.Y branch is created and pushed
19. Version increments the minor number correctly (e.g. 1.0.0 will become after a minor version: 1.1.0)
20. GitHub release created targeting main

major workflow
21. major release from main succeeds
22. version increments the major number correctly (e.g. 1.2.3 after a major version is 2.0.0)
23. branch named releases/release-X.0 (e.g. releases/release-2.0) is created

Packaging
24. build locally without passing assembly version uses gitversion derived version
25. msi installs cleanly on a fresh machine
26. msi upgrades a previous installation (old version is removed)
27. installed exe is on path and callable

gitversion 
28. On a WI branch, version has a prerelease label (e.g. 0.0.1-WI01234.1+...)
29. On main, version has no prerelease label

other:
- major running from non main branch fails
- this might be difficult to test, but if github repo is missing secrets the action should fail
- msi local build should get a real gitverison (not the default 0.0.0.0)
- BuildChocolateyPackage.ps1 is no longer triggered on every local MSI build building the wixproj locally does NOT produce a .nupkg

The paradox of low level skills

- adults are generally better at higher level thinking and logic, this allows us to be more present when developing skills- be conscious about mistakes and so on. more life experience so easier to understand consciously abstract concepts. especially formal stuff, like advanced math, not intangible stuff like pattern recognition.
- This makes us think we should be better at learning when we are adults
- But kids not only have an abundance of time, they have no respect for their time -> a kid can spend 100s of hours shamelessly grinding out something just because they like it
	- coding
	- CAD
	- guitar chords
	- grinding out low level mathematics computations such as solving differential equations, integrals, matrix multiplications
- 