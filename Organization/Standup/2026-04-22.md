https://github.com/WiseTechGlobal/CargoWise/pull/43392

Tasks
1) We have rewritten a large part of the PR to forget about schema owners and instead just do the simple task of recreating custom users. First thing to do is confirm that the mapping method from cargowise data: `EnsureDbLoginsCorrectlyMappedToAllDatabases` does NOT create users, and if it does, that it does NOT map the enterprise users to logins (in our case, the case of database developers). [DONE]
2) Put the PR link at the top of todays notes for quick reference [DONE]
3) Skim the PR and make sure it has all teh changes, clean it up
4) clean up tests
5) Rebuild
6) Copy Dlls to VM
7) Functional test by running DBBR

Thinking:

```
a few changes are needed. for these changes you need to determine whether the restore is happening on top of an existing database or the restore is fresh and the existing database does not exist. This is already done in the tool so identify where this happens so we can use this information.
1) we want to rename all users and logins. Currently in the method GetEnterpriseDbUsersInDatabase we decide the login to be created by just reusing the user name from the source DB. can we update this so the user and login to be created is named loginPrefix_{TargetDatabaseName}_{whatever was in the original database} so for example EnterpriseDbUser_SourceSystem_me becomes EnterpriseDbUser_TargetSystem_me
2) make the password for this login a random password
3) IF the db we are restoring does not already exist in the database, and we are doing a fresh restore, delete all the old users. If the restored db does already exist, do not drop the existing users.
```


TODO:
1) PR for moving between OUs code review and self functional test [IN PROGRESS]
	1) [[WI00782542 - CCC - Supports Multiple CW1 OUs]]
	2) https://github.com/WiseTechGlobal/CargoWiseCloud.Console/pull/209/changes



---

1) fix the fucking db schema in console- pull, merge, run frontend tests [IN PROGRESS]
	1) go over changes in PR
	2) submit for shelf test and review
2) clean up OU WI for review, functional test it
