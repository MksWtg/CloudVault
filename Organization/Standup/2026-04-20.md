
What have we done in the morning?
- Functionally tested the reachability DBBR secondary replicas change
- Cleaned up PR, tests, submit for shelf

What do we want to do now?
- Functionally test the other one, CP2T user repository db

#### CP2T issue recap
To recap the issue real quick:
- user repository is a db which has client specific sql objects, specific tables, specific db users.
- we can create users in this database via database developer by logging into registered cw instance
- this creates a login and a user in localhost and the user repository database 
- when we backup this database and restore it to another server (e.g. on a VM!!) the user which is per db is passed on through the .bak but the login which is per server is not.


Tasks
1) reproduce the error [DONE]
	1) Take a backup [DONE]
	2) Restore backup onto other server [DONE]
	3) Check if the per server principals are missing [DONE]
		1) Open SSMS, connect to the test server [DONE]
		2) Query db principals in user repo, expect `mukund` user [DONE]
		3) Query server principals, expect `mukund` to not be there [DONE]
2) test the fix [IN PROGRESS]
		1) Run DBBR on the backup that has the `mukund` user [IN PROGRESS]
		2) 

TODO:
1) 
2) PR for moving between OUs code review and self functional test [IN PROGRESS]
	1) [[WI00782542 - CCC - Supports Multiple CW1 OUs]]
	2) https://github.com/WiseTechGlobal/CargoWiseCloud.Console/pull/209/changes



---

Tasks

TODO:
1) figure out if DBBR can restore a bak to multiple replicas in AG [DONE]- it can
	1) does it check at the start if they can all access the .bak from shared network [DONE] it does not lmao
	2) why does shared network not appear in the tool [DONE] ans: it assumed it is because you have to select the file. however, this seems like a fundamental flaw in the tool.
		1) make this change [DONE]
		2) create a work item with adequate description, clean up PR, shelf test [IN PROGRESS]
2) Fix the old PR to onyl make changes to user repository and shelf test this (cancel old dbbr shelf test) [DONE]
3) fix the fucking db schema in console- pull, merge, run frontend tests [IN PROGRESS]
	1) go over changes in PR
	2) submit for shelf test and review
4) fix bdd for doc util [DONE]
5) add time recording for last 8 hours [DONE]
6) clean up OU WI for review, functional test it
7) start on other stuffs :D ask james, cart, zeynep


couple functional reviews right now, one for DBBR and one for console
if i get through those, resolving submission failure for one of the console schema change items